# Core runtime dependencies
pillow
numpy
pytesseract
streamlit
huggingface-hub

# Optional: LLM post-processing (falls back gracefully if unavailable)
llama-cpp-python

# Dev / CI only
ruff
mypy
bandit
pip-audit
pytest
pytest-cov

# gitleaks (install separately: https://github.com/gitleaks/gitleaks/releases)
pydantic
