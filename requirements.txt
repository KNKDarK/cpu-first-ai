# Core runtime dependencies
pillow
numpy
pytesseract
streamlit
huggingface-hub

# Optional: LLM post-processing (commented out so Streamlit Cloud doesn't crash during pip install)
# llama-cpp-python

# Dev / CI only
ruff
mypy
bandit
pip-audit
pytest
pytest-cov
pydantic
